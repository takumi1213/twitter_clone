<nav class="navbar navbar-expand-lg navbar-light bg-dark">
<div class="container">
      <a class="navbar-brand text-white" href="/">Awesome Blog</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
           <%= link_to raw("Home<span class='sr-only'>(current)</span>"), root_url, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
           <%= link_to "About Us", pages_about_url, class: "nav-link text-white" %>
          </li>
          <li class="nav-item">
           <%= link_to "Contact", pages_contact_url, class: "nav-link text-white" %>
          </li>
          <li class="nav-item dropdown">

        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Account
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if logged_in? %>
              <%= link_to current_user.name , user_path(current_user), class: "dropdown-item"%>
              <%= link_to "Edit Profile", edit_user_path(current_user), class: "dropdown-item"%>
              <%= link_to "Users", users_path, class: "dropdown-item"%>
              <div class="dropdown-divider"></div>
              <%= link_to "Logout", logout_path, class: "dropdown-item"%>
          <% else %>
              <%= link_to "Sign Up", signup_path, class: "dropdown-item"%>
              <div class="dropdown-divider"></div>
              <%= link_to "Login", login_path, class: "dropdown-item"%>
          <% end %>
        </div>
      </div>
    </nav>
</div>